import 'package:flutter/material.dart';

import 'works_scaffold.dart';

class WorksRoute extends StatelessWidget {
  static const route = '/works';
  @override
  Widget build(BuildContext context) {
    return WorksScaffold();
  }
}
